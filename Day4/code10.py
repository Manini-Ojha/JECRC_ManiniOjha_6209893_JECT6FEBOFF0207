#Continue keyword

#skipping an iteration at a specific condition

#Pass keyword

#if block requires statements and if we don't have a statement block then an error will occur so to allow an empty if block, we use pass, same for for loops, classes and functions.
#it is used to make an abstract method
#range (from the for loop syntax) is an iterable object
